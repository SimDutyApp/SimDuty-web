# SimDuty-web

Static site for SimDuty (HTML/CSS/JS). No build pipeline.

## Preview
- Open `index.html` directly, or run a static server:
  - `python -m http.server 8000`

## Recent updates
- Roadmap updated to Phases 1-6 with Beta & Integrations.
- Added Discord links + CTA for alpha-tester invites.
- Mobile header with hamburger menu.
